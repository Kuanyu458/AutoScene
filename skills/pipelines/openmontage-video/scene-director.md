# Scene Director — openmontage-video

Map every script section to a source asset or declarative browser flow. For
2D UI, record the click selector, result region, and one focus event maximum;
the focus scale must stay within `job.edit.focus_scale` (default 1.20–1.35).

For 3D UI, describe at least two independent planes, a real perspective value,
translateZ/rotation changes, and one directional motion. A full-frame slide,
global shake, repeated zoom pulse, or decorative 3D label is not evidence.
List nominal cut times and whether each is semantic-locked; the edit stage may
snap only unlocked times to the audiomap within the declared tolerance.
